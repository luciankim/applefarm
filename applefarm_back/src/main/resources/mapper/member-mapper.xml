<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.or.iei.member.model.dao.MemberDao">
    <select id="selectAddressCount" resultType="int">
    	select count(*) from address_tbl where member_no=#{memberNo}
    </select>
	<update id="updateAddressDefault">
		update address_tbl set address_default = 0 where member_no=#{memberNo}
	</update>
	<insert id="insertAddress">
		insert into address_tbl values(address_seq.nextval,#{memberNo},#{zipcode},#{address},#{addressDetail},#{addressName},#{addressPhone},#{addressRequest},#{addressDefault})
	</insert>	
	<select id="selectAddressBasic" resultType="address">
		select * from address_tbl where member_no=#{memberNo} and address_default=1
	</select>
	<select id="addressNoBasicTotalCount" resultType="int">
		select count(*) from address_tbl where member_no=#{memberNo} and address_default=0
	</select>
	<select id="selectAddressList" resultType="address">
		select * from (select rownum as rnum, a.* from (select * from address_tbl where member_no=#{memberNo} and address_default=0 order by 1 desc)a) where rnum between #{start} and #{end}
	</select>
	<select id="selectAddress" resultType="address">
		select * from address_tbl where member_no=#{memberNo} order by address_default desc,address_no desc
	</select>
	<delete id="deleteAddress">
		delete from address_tbl where address_no=#{addressNo}
	</delete>
	<update id="updateAddressDefault1">
		update address_tbl set address_default = 1 where address_no=#{addressNo}
	</update>
</mapper>