<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.or.iei.member.model.dao.MemberDao">
  
  
  
  <select id="selectOneEmail" resultType="int">
  
  
  select count(*) from member_tbl where member_email=#{memberEmail}
  
  </select>
  
  <select id="selectOneId" resultType="int">
  
  select count(*) from member_tbl where member_id=#{memberId}
  
  </select>
  
  <select id="selectOneNickName" resultType="int">
  
  select count(*) from member_tbl where member_nickname=#{memberNickName}
  
  
  </select>
  
  <insert id="join">
  
  	insert into member_tbl values (MEMBER_SEQ.nextval,#{memberName}, #{memberNickName}, #{memberId}, #{memberPw}, #{memberEmail}, #{memberPhone}, 1,#{memberAccountnumber}, sysdate, 37,1,0)
  
  </insert>
  
 
  <select id="selectId" resultType="member">
  
  	select * from member_tbl where member_id=#{memberId}
  
  </select>
  
  
</mapper>
